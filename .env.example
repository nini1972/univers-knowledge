OPENAI_API_KEY=your_openai_api_key_here
# Can be configured for other providers supported by LiteLLM/CrewAI

# Set to true only after running `crewai login` to enable cloud trace upload.
# Requires authentication; leave false for local runs.
CREWAI_TRACING_ENABLED=false
